# Default values for capi-metal3
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

image:
  repository: registry.opensuse.org/isv/suse/edge/clusterapi/containerfile/suse/cluster-api-provider-rke2-bootstrap
  pullPolicy: IfNotPresent
  tag: "0.2.6"


